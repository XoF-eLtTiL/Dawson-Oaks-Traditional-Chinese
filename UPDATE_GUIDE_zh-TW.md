# Dawson Oaks Trailer Park 繁體中文安裝與更新教學

適用版本：繁體中文 `v1.0.20`

## 套件內容

完整安裝包已包含 BepInEx 6 IL2CPP、XUnity AutoTranslator 5.6.2 IL2CPP、人工校正繁體中文翻譯、中文字體、繁體中文 Logo、中文輸入與聊天修補 DLL，以及手動翻譯更新工具。

XUnity 線上自動翻譯已關閉。套件不會放入 `_AutoGeneratedTranslations.txt` 等自動生成翻譯。

## 找到遊戲目錄

1. 開啟 Steam 收藏庫。
2. 在 **Dawson Oaks Trailer Park** 上按滑鼠右鍵。
3. 選擇 **內容 → 已安裝檔案 → 瀏覽**。
4. 開啟的資料夾內應該能看到 `Dawson Oaks Trailer Park.exe`。

預設位置通常是：

```text
C:\Program Files (x86)\Steam\steamapps\common\Dawson Oaks Trailer Park
```

## 全新安裝

1. 完全關閉遊戲。
2. 從 [v1.0.20 Release](https://github.com/XoF-eLtTiL/Dawson-Oaks-Traditional-Chinese/releases/tag/v1.0.20) 下載 `DawsonOaks_TraditionalChinese_v1.0.20.zip`。
3. 解壓縮後，將裡面的所有檔案與資料夾複製到遊戲根目錄。
4. Windows 詢問是否合併資料夾或覆蓋同名檔案時，選擇允許。
5. 確認沒有多包一層資料夾。

正確結構如下：

```text
Dawson Oaks Trailer Park\
├─ Dawson Oaks Trailer Park.exe
├─ winhttp.dll
├─ doorstop_config.ini
├─ BepInEx\
├─ 手動更新翻譯.bat
└─ tools\Update-DawsonOaksTranslation.ps1
```

6. 從 Steam 啟動遊戲。第一次啟動會建立 IL2CPP 相容檔案，所需時間會比平常長。
7. 看到繁體中文介面與繁體中文 Logo 即代表載入成功。

## 覆蓋更新既有版本

1. 關閉遊戲。
2. 將新版完整安裝包全部覆蓋到原本的遊戲根目錄。
3. 啟動遊戲。

主修補 DLL 會檢查 GitHub 更新；遠端版本較舊時不會覆蓋較新的本機 DLL。人工翻譯檔不會自動更新，以免覆蓋本機校正內容。

## 手動更新翻譯

1. 關閉遊戲。
2. 在遊戲根目錄雙擊 `手動更新翻譯.bat`。
3. 等待視窗顯示「翻譯更新完成」或「目前已是最新版翻譯」。
4. 按任意鍵關閉更新視窗，再啟動遊戲。

更新工具會：

- 從本專案下載最新版人工翻譯與 `SHA256SUMS.txt`。
- 驗證 UTF-8、檔案大小、行數、翻譯格式與 SHA-256。
- 將舊翻譯備份到 `BepInEx\Translation\zh-TW\Text\Backups`。
- 全部驗證通過後才替換正式翻譯檔。
- 下載或驗證失敗時保留現有翻譯。

也可以在 PowerShell 手動指定遊戲位置：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\tools\Update-DawsonOaksTranslation.ps1" -GameDir "C:\Program Files (x86)\Steam\steamapps\common\Dawson Oaks Trailer Park"
```

## 更換中文字體

1. 將 TTF 或 OTF 字體放進 `BepInEx\plugins\DawsonOaks.ZhTW\Assets`。
2. 編輯 `BepInEx\config\tw.a9904.dawsonoaks.zhtwpatcher.cfg`。
3. 將 `FontFileName` 改成字體完整檔名。
4. 儲存後返回遊戲；DLL 會重新載入字體。

字體必須包含繁體中文字形。原套件另附 Noto Sans CJK TC 作為罕見字備援。

## 多人聊天

- v1.0.20 會將長中文訊息安全分段，接收完成後顯示為一則完整訊息。
- 收發雙方都應安裝 v1.0.20 或更新版本。
- 英文與數字仍使用遊戲原本的聊天流程。

## 移除中文化

1. 關閉遊戲。
2. 執行遊戲根目錄的 `移除繁中.bat`。
3. 選項 1 只移除繁中翻譯、字體、Logo 與 Dawson Oaks 修補，保留 BepInEx/XUnity。
4. 選項 2 完整移除套件內的 BepInEx/XUnity 與載入器。

選項 2 也會移除 BepInEx 裡的其他模組；若你另外安裝過其他模組，請先備份。

## 故障排除

### 完全沒有中文

- 確認 `winhttp.dll` 與遊戲 EXE 位於同一層。
- 開啟 `BepInEx\LogOutput.log`，搜尋 `Loaded Dawson Oaks Traditional Chinese Patcher 1.0.20`。

### Logo 換場景後恢復原圖

- 確認 `DawsonOaks.ZhTWPatcher.dll` 的檔案版本是 `1.0.20.0`。
- 日誌應出現 `Scene '...' activated; enforcing the replacement logo...`。
- 用最新版完整安裝包重新覆蓋 `BepInEx\plugins\DawsonOaks.ZhTW`。

### 中文輸入選字後沒有文字

- 先用視窗模式或全螢幕無邊框模式測試。
- 輸入欄位取得焦點後，日誌應出現 `Windows IME composition enabled`。

### 中文字變成方塊

- 確認 `Assets` 內仍有 `Iansui-Regular.ttf` 與 `NotoSansCJKtc-Regular.otf`。
- 若自行換字體，確認新字體包含繁體中文字形。

### 手動更新翻譯失敗

- 確認遊戲已完全關閉。
- 確認電腦能連線到 `raw.githubusercontent.com`。
- 不要只移動 BAT；它需要同一套件內 `tools` 資料夾的 PowerShell 腳本。
